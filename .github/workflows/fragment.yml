name: blank

on:
  push:

jobs:
  ingredient:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        misguide: ["mechanics", "bankrupt", "stove", "depend", "agony"]
        discuss: ["recover", "specialize", "subtract", "dull"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar ot
